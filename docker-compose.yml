services:
  media_bot:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - TZ=Asia/Dhaka
    restart: always
    network_mode: "host"  # <--- ADD THIS LINE HERE
    volumes:
      - .:/app
